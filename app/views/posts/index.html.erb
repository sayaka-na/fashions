<section class="hero">
  <div class="hero-body">
    <div class="container">
      <h1 class="title">
        投稿一覧
      </h1>
    </div>
  </div>
</section>

<%= render 'posts/posts', posts: @posts %>

<div class="paginate">
<%= paginate @posts %>
</div>